<UserControl x:Class="TravelApp.Views.ParkView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TravelApp.Views"
        mc:Ignorable="d"
       >
    <Grid>
        <Button VerticalAlignment="Bottom" Command="{Binding HiButtonCommand}" 

        CommandParameter="Hai" Content="{Binding HiButtonContent}"

                Width="100"

                Height="100"  />

        <Button Content="Toggle Can Click" 

        Command="{Binding ToggleExecuteCommand}"  Width="100" Height="100"/>
    </Grid>
</UserControl>
